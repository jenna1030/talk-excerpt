# 톡갈피 · 첫 버전

`index.html`을 브라우저로 여세요. 설치나 빌드가 필요 없습니다. 네트워크가 없어도 기본 글꼴로 작동합니다.

## 사용 흐름

1. 등장인물을 추가하고 이름, 좌우 위치, 프사 URL, 색상을 설정합니다.
2. 대화를 추가하거나 기존 대화 아래에 끼워 넣습니다. 수정, 삭제, 순서 변경, 실행 취소를 지원합니다.
3. 연속된 대화의 체크박스를 선택하고 접기로 묶습니다. 내용은 유지되며 출력에서 기본 닫힘입니다. 구간 내부 순서 변경을 지원하며, 구간 밖으로 옮기려면 묶음을 먼저 해제하세요.
4. 대화 수정 창에서 같은 발췌문 또는 다른 글의 원문에 답장을 연결합니다.
5. 프로젝트를 JSON 파일로 저장합니다. 브라우저 자동 저장은 보조 수단입니다. 브라우저나 파일 위치가 바뀌면 저장 데이터가 달라질 수 있습니다.
6. HTML 내보내기로 복사한 코드를 티스토리 본문의 HTML 모드에 넣습니다.

## 글 사이의 연결

원본 글의 게시 주소를 입력한 다음 메시지의 링크 복사를 누릅니다. 다른 프로젝트의 답장에서 '다른 글의 대화'를 선택하고 링크, 작성자, 인용문을 입력하세요. 인용문은 수동 입력이며 원본 수정이 다른 글에 자동 반영되지는 않습니다.

원본 글에 돌아갈 링크를 달려면 원본 대화의 '돌아가기 링크'에 `2번 글 답장 보기 | https://블로그주소/2#메시지ID` 형식으로 입력합니다. 링크는 여러 줄을 지원합니다. 두 글에 각각 새 HTML을 붙여 넣어야 합니다. 방문 경로에 따른 자동 복귀는 제공하지 않습니다.

접힌 메시지로 향하는 링크는 독자가 펼칠 수 있도록 접기 구간의 상단을 가리킵니다. 공유 후 구간을 해제하거나 삭제하면 기존 구간 링크는 갱신해야 합니다. 메시지 ID는 수정/끼워넣기/순서 변경 시 유지됩니다. 원본 삭제 시 같은 프로젝트의 답장 연결은 확인 후 해제됩니다. 다른 프로젝트의 링크는 자동 추적하지 않습니다.

## 범위와 확인 사항

- 인원 수 고정 제한 없음. 대량 대화는 기기 성능에 영향을 받습니다.
- 프사는 공개 접근 가능한 http(s) 이미지 주소 방식입니다. 로컬 이미지 업로드/호스팅은 아직 지원하지 않습니다.
- 본문 출력은 인라인 스타일, HTML 링크, details/summary만 사용하며 스크립트와 스킨 설치가 필요 없습니다.
- 실제 티스토리 저장 과정에서 HTML이 유지되는지 검증되지 않았습니다. 비공개 테스트 글에서 PC/모바일 접기, 링크, 프사, 편집 후 재저장을 확인하세요.
- 완전 생략, 중첩 접기, PNG 출력은 현재 제공하지 않습니다.
- JSON은 외부 서버로 전송하지 않습니다. 프사와 선택적인 Google Fonts 요청은 외부 네트워크를 사용합니다.
- 공개 저장소에는 실제 개인 대화나 사적인 사진을 커밋하지 마세요.

참고 페이지: https://mok0999.github.io/315/chat.html — 웹 조회 도구에서는 접근하지 못했지만, 이후 사용자가 열어둔 Chrome 탭에서 화면과 기능 목록을 확인했습니다. 원본 코드는 가져오지 않았고 화면 구성도 복제하지 않았습니다. 현재 버전은 채팅/접기/답장/HTML 출력 중심이며 참고 도구의 글 서식, 사진 메시지, 여러 출력 모드를 모두 구현한 것은 아닙니다.

검증: JavaScript 문법 및 시작 렌더링, 기본 닫힘 접기, 접힌 원문 링크, HTML 이스케이프, URL 필터, 프로젝트 검증에 대한 코드 수준 확인을 통과했습니다. 로컬 파일의 브라우저 열기는 도구 정책에 차단되어 시각적 검증은 완료하지 못했습니다.

## 카카오톡 TXT 붙여넣기

대화 편집에서 `카톡 TXT 붙여넣기`를 누르고 원문을 붙여 넣은 뒤 `대화 분석하기`를 선택하세요. `.txt` 파일 직접 선택도 지원합니다(UTF-8, EUC-KR). PC 카카오톡의 `[이름] [오후 3:15] 내용`과 날짜 구분선 형식을 지원하며 여러 줄 메시지를 유지합니다. 다른 모바일 내보내기 형식은 현재 지원하지 않습니다.

인물별로 기존 인물에 연결하거나 새 인물로 추가할 수 있습니다. 이름이 정확히 하나 일치하면 기존 인물을 기본 선택합니다. 새 인물 중 오른쪽에 배치할 인물을 선택할 수 있습니다. 대화 추가/전체 교체 모두 실행 취소가 가능합니다. 날짜는 메시지별로 저장·표시되며 수정할 수 있습니다.

사진, 파일, 이모티콘은 원본 TXT에 있는 안내 문구로만 가져옵니다. 프사와 답장 관계는 TXT에서 복원하지 않습니다. 인식하지 못한 메시지 밖의 줄은 확인 및 제외 동의 후 가져옵니다. 메시지 안에서 시작 형식과 일치하는 줄은 새 메시지로 해석되므로 붙여넣기 미리보기를 확인하세요. 실제 대화 파일은 프로젝트에 포함하지 않습니다.

초기 화면은 인물과 대화가 없는 빈 상태입니다. 기존 브라우저 자동 저장 내용은 유지되므로 이전에 저장된 예시가 있다면 편집기에서 해당 대화와 인물을 삭제하세요.
